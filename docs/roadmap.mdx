---
title: "Roadmap"
description: "Implemented phases, local hardening evidence, and approval-gated release work."
---

Phases 1–8 form the current implemented renderer, local-only reference application, and Mintlify documentation source. Engine compatibility version `0.0.0-phase.8` carries the bounded image/section/header/footer/page-field profile forward and partitions render caches by exact compatibility inputs. Convex foundations remain tested but are not mounted in the public playground. Local Phase 9 hardening evidence, a verified Vercel deployment, and the published `0.1.0-next.0` npm package set are present; this is not a claim of broad Word compatibility, complete browser-matrix proof, production authentication, persistent SaaS operation, a stable release, or provenance on the one-time direct bootstrap publication.

## Phase 4 — Implemented; hardening ongoing

- Relationship-owned numbering definitions and overrides; supported bullet, decimal, letter, Roman, multilevel/custom, and legal labels.
- Searchable source-linked labels, counter continuation/restarts, list indentation/wrapping, keep chains, keep-lines-together, and widow/orphan control.
- Nested whole-paragraph `if`/optional `else`/`each`, item-relative object fields, safe formatters, canonical loop manifest paths, deterministic nested schema/starter data, and cumulative limits.
- Explicit locale/time-zone formatter context with no ambient locale, time zone, or clock dependency.

## Phase 5 — Implemented; hardening ongoing

- Positive integer-twip `tblGrid` geometry with exact explicit `tblW`/`tcW` equality and deterministic default margins.
- Supported borders, clear-pattern solid shading, exact/at-least row heights, vertical alignment, `gridSpan`, and validated `vMerge` chains.
- Dedicated whole-row `if`/optional `else`/`each` blocks with structural header/merge safeguards and cumulative limits.
- Contiguous repeated headers, deterministic row fragmentation and `cantSplit` fallback diagnostics, source-linked traces, multi-page searchable output, and deterministic PDF strokes.
- Explicitly excluded: percentage widths, nested tables, table styles/themes, complex shading, and complete Word autofit. Static images in supported cell paragraphs arrive in Phase 6.

## Phase 6 — Implemented; hardening ongoing

- Internal relationship-owned PNG/JPEG content with explicit positive inline dimensions, immutable bytes, source links, exact MIME/signature/dimension/profile validation, and count/byte/side/pixel/decoded-work limits.
- Canonical `{{@image path}}` render values with explicit PNG/JPEG bytes, pixel dimensions, physical twip bounds, deterministic resource IDs, generated schema/starter data, optional aspect preservation, and semantic alt text.
- Deterministic image preparation and exact-content deduplication; PDF image XObjects use DCT for JPEG, Flate for normalized PNG planes, and grayscale `/SMask` objects for alpha.
- Multiple deterministic `nextPage` sections with portrait/landscape page geometry and matching per-page PDF `MediaBox` values.
- Inherited default headers/footers with exact edge-relative distances (720-twip defaults), static images, reusable template values/formatters/paragraph blocks, and global decimal `PAGE`/`NUMPAGES` fields.
- Binary-safe workspace-PDF validation that follows classic xref offsets and stream lengths, validates upright text/image resources, and extracts searchable per-page text.
- Explicitly excluded: non-canonical image tags, image fetches, anchors/floating placement, crop, rotation, SVG, broad image profile/color conversion, tagged-PDF accessibility metadata, continuous/odd/even breaks, first/even header variants, automatic header/footer numbering, and arbitrary fields.

## Phase 7 — Implemented; hardening ongoing

- Anonymous browser sessions through `convex-helpers`, with every template, render, cache read, and delete scoped to the session.
- Direct generated upload URLs for browser-to-Convex DOCX/PDF storage; storage IDs, bounded metadata, diagnostics summaries, and deterministic hashes remain in database documents.
- Cursor-paginated history, bounded realtime recent status, completed-PDF cache reuse, owned bearer-URL reads, and scheduled cascading deletion of rows plus storage objects.
- Explicitly excluded: mounting persistence in the public playground, production authentication/tenant policy, perfect abandoned-upload reclamation, rate limiting, retention compliance, and treating storage URLs as fine-grained authorization.

## Later-phase status

### Phase 8 — Landing page and documentation

Implemented and locally browser-verified: polished Geist Mono landing page, Mintlify documentation, interactive examples, architecture explanation, responsive support matrix, GitHub/community links, and SEO/PWA metadata.

### Phase 9 — Hardening

Local evidence is implemented: hostile-input tests, a reproducible fast-check property gate plus fixed-seed structural corpora, resource-limit and cancellation coverage, exact golden PDF/trace parity across separate Bun processes, Node 24, and a real Chromium module worker, zero-violation automated axe scans for covered light/dark desktop/mobile states, performance benchmarks, enforced publication-tarball size budgets, public API review, and pull-request dependency review. A Vercel Bun deployment now has hosted landing/sample/K3/axe evidence. Broader licensed Microsoft Word and Google Docs-exported fixtures remain open.

### Phase 10 — Release preparation

The first npm prerelease is published: all 11 packages are available at `0.1.0-next.0`, registry-only Bun and Node imports pass, registry signatures verify, and every package is bound to the protected GitHub trusted publisher. The next automated prerelease must prove the GitHub OIDC provenance path; stable release remains gated on the complete licensed editor fixture corpus and a separate stability decision.

Each feature must enter through the staged architecture. Current focused tests and hosted smoke do not establish broad Microsoft Word fixture coverage, full Bun/Node/browser equivalence, broad image/color-profile equivalence, production authentication, persistent SaaS operation, or production readiness.
