---
"@apexmed/forms": minor
---

Add the public `@apexmed/forms` package: a headless form model with tag binding and an Apex adapter. FormBuilder and FormRuntime use TanStack Form and ship as workspace plus shadcn-registry UI. The umbrella now ships a `bind-apex-form` agent skill and Mintlify forms documentation.
